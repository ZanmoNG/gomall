// idl/hello/hello.proto
syntax = "proto3";

package fronted.home;

option go_package = "fronted/home";

import "frontend/common.proto";
import "api.proto";

message Empty {}

service HomeService {
  rpc Home(common.Empty) returns(common.Empty) {
    option (api.get) = "/";
  }
}
