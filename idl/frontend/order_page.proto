syntax = "proto3";

package fronted.order;

import "api.proto";
import "frontend/common.proto";

option go_package = "/fronted/order";

service OrderService {
  rpc OrderList(common.Empty) returns (common.Empty) {
    option (api.get) = "/order";
  }
}